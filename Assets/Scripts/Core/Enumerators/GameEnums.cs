public enum GameState
{
    MainMenu,
    Playing,
    Paused,
    GameOver,
    Victory
}

public enum BlockType
{
    Start,
    Finish,
    Walkable,
}

public enum MovementSpeed
{
    Normal,
    Fast
}
